# This is an example environment file.
# Copy this file to .env and fill in your actual values.

# Flask Secret Key: Used for session management, signing tokens, etc.
# Generate a strong random key for production. For PoC, a simple string is fine.
# Example: openssl rand -hex 32
SECRET_KEY='a_very_secret_key_for_poc_change_me_!@#$%'

# Database URL for SQLAlchemy
# For SQLite, this is the path to the database file.
# 'sqlite:///inventory_poc.db' means the file 'inventory_poc.db' will be in the root project directory.
DATABASE_URL='sqlite:///inventory_poc.db'

# Other potential configurations:
# FLASK_ENV='development' # or 'production'
# DEBUG=True # or False
